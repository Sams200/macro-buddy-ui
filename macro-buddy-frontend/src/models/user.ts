export interface User {
    userId: number;
    username: string;
    email: string;
    isAdmin: boolean;
}

export interface UserResponse {
    userId: number;
    username: string;
    email: string;
    isAdmin: boolean;
}

